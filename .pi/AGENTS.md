# serious-nixos-config

Nix home-manager configuration for macOS.

## Pi Agent Harness

Pi configuration lives in `home-manager/files/pi/`. When adding, removing, or
modifying extensions or skills, update `home-manager/files/pi/README.md` to
keep the tables in sync.
